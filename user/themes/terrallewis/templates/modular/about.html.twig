<div class="slab">
	<section id="about">
	    <div class="row">
	        <div class="three columns wow bounceInLeft" data-wow-delay=".25s">
		        <div>
		            <img src="{{ page.media.images['profilepic.jpg'].url }}" alt=""  class="profile-pic" />
		        </div>        
	        </div>
	        <div class="nine columns main-col wow bounceInRight" data-wow-delay=".25s">
	            {{ page.content }}	        
	            <div class="row">
	                {% if page.header.buttons %}
	                <div class="columns download">
	                    <p>
	                        {% for button in page.header.buttons %}
	                            <a href="{{ button.url }}" class="button"><i class="fa fa-{{ button.icon }}"></i>{{ button.text }}</a>
	                        {% endfor %}
	                    </p>
	                </div>
	                {% endif %}
	            </div>
	        </div>
	    </div>
	</section>
</div>
